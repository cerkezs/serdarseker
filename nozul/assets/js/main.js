document.addEventListener('DOMContentLoaded', () => { const mnav = document.getElementById('mnav'); const mmenu = document.getElementById('mmenu'); if (mnav && mmenu) { mnav.addEventListener('click', () => { mmenu.classList.toggle('hidden'); }); } const currentPath = window.location.pathname; const desktopLinks = document.querySelectorAll('.nav-link'); desktopLinks.forEach(link => { let isActive = false; if (link.tagName === 'A') { const linkUrl = new URL(link.href, window.location.origin); const isHome = (currentPath === '/' || currentPath.endsWith('/index.html')) && linkUrl.pathname.endsWith('/index.html'); const isExactMatch = currentPath === linkUrl.pathname && currentPath !== '/'; const isUrunlerParent = currentPath.includes('/urunler/') && linkUrl.pathname.endsWith('urunler.html'); isActive = isHome || isExactMatch || isUrunlerParent; } else if (link.tagName === 'BUTTON' && link.textContent.trim().includes('Sektörler')) { isActive = currentPath.includes('/sektor/'); } if (isActive) { link.classList.remove('text-gray-700', 'hover:text-primary', 'hover:bg-gray-50'); link.classList.add('text-primary', 'font-bold', 'active'); } else { link.classList.remove('text-primary', 'font-bold', 'active'); link.classList.add('text-gray-700', 'hover:text-primary', 'hover:bg-gray-50'); } }); const fullHref = decodeURIComponent(window.location.href).replace(/\\/g, '/'); const isSektor = fullHref.includes('/sektor/'); if (isSektor) { const currentFile = fullHref.split('/').pop().split('?')[0]; const dropdownLinks = document.querySelectorAll('nav a[href*="sektor"]'); dropdownLinks.forEach(link => { const rawHref = link.getAttribute('href') || ''; const linkFile = rawHref.split('/').pop(); if (linkFile && currentFile && linkFile === currentFile) { link.classList.add('sektor-active'); } }); } const sliderContainer = document.getElementById('hero-slider-container'); const slides = document.querySelectorAll('.hero-slide'); const dotsContainer = document.getElementById('hero-slider-dots'); if (slides.length > 0) { let currentSlide = 0; let slideInterval; if (dotsContainer) { slides.forEach((_, index) => { const dot = document.createElement('button'); dot.className = `slider-dot${index === 0 ? ' dot-active' : ''}`; dot.setAttribute('aria-label', `Slide ${index + 1}`); dot.addEventListener('click', () => goToSlide(index)); dotsContainer.appendChild(dot); }); } const updateSlider = () => { slides.forEach((slide, index) => { if (index === currentSlide) { slide.classList.add('active'); } else { slide.classList.remove('active'); } }); if (dotsContainer) { const dots = dotsContainer.children; Array.from(dots).forEach((dot, index) => { if (index === currentSlide) { dot.classList.add('dot-active'); } else { dot.classList.remove('dot-active'); } }); } }; const nextSlide = () => { currentSlide = (currentSlide + 1) % slides.length; updateSlider(); }; const goToSlide = (index) => { currentSlide = index; updateSlider(); resetInterval(); }; const startInterval = () => { slideInterval = setInterval(nextSlide, 4000); }; const resetInterval = () => { clearInterval(slideInterval); startInterval(); }; startInterval(); if (sliderContainer) { sliderContainer.addEventListener('mouseenter', () => clearInterval(slideInterval)); sliderContainer.addEventListener('mouseleave', startInterval); } } const counters = document.querySelectorAll('.counter[data-target]'); if (counters.length > 0) { const observer = new IntersectionObserver((entries, observer) => { entries.forEach(entry => { if (entry.isIntersecting) { const counter = entry.target; const target = parseInt(counter.getAttribute('data-target'), 10); const duration = 2000; let startTime = null; const step = (timestamp) => { if (!startTime) startTime = timestamp; const progress = Math.min((timestamp - startTime) / duration, 1); const easeOut = 1 - Math.pow(1 - progress, 3); counter.innerText = Math.floor(easeOut * target); if (progress < 1) { window.requestAnimationFrame(step); } else { counter.innerText = target; } }; window.requestAnimationFrame(step); observer.unobserve(counter); } }); }, { threshold: 0.5 }); counters.forEach(counter => { observer.observe(counter); }); } });